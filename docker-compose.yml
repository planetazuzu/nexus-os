version: '3.8'

services:
  nexus-os:
    build: .
    container_name: nexus-dashboard
    ports:
      - "3333:3334"
    environment:
      - NEXUS_TOKEN=nexus-secret-2026
      - DATABASE_URL=postgresql://blackbox:blackbox123@blackbox-postgres-1:5432/nexus
    restart: unless-stopped
    networks:
      - blackbox-network

networks:
  blackbox-network:
    external: true
